// Narrative barrel — single entry for the prose surface (tie pool,
// action templates, defaultNarrator). Server / sim / client all reach
// the narration through this barrel via @xdyb/shared.
export * from './lines.js';
